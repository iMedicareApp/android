<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.EditText" parent="ThemeOverlay.MaterialComponents">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorAccent">@color/background1</item>
    </style>

    <style name="AppTheme.Spinner" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textColor">@color/background1</item>
        <item name="android:textColorHint">@color/background1</item>
        <item name="android:textColorSecondary">@color/background1</item>
    </style>

    <style name="TextAppearance.Design.Hint.TextHint" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">10sp</item>
    </style>
</resources>